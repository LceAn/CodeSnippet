# 未来更新计划（Roadmap）

> 最近更新：2026-08-30。本文件描述代码片段库的扩充方向，按优先级排列。

## 短期

- 为 Flask 应用工厂示例补充常见扩展的接入示例（如 SQLAlchemy 初始化、蓝组织化路由），保持"可修改的起点"定位。
- 为 `Shell/` 模板接入 ShellCheck 的本地校验脚本，与现有单元测试并列。
- 在 README 的内容索引中为每个片段补充"适用场景 / 不适用场景"说明。

## 中期

- 按语言扩充目录（如 Go、SQL、Docker Compose），每个片段保持"最小可运行 + 单元验证"的标准。
- 增加片段索引生成脚本，从目录结构自动刷新 README 内容索引表，避免手工维护漂移。

## 长期 / 想法

- 当片段数量明显增长时，考虑拆分为独立示例仓库或启用 GitHub Pages 做索引页。
- 评估是否引入轻量 CI 矩阵，覆盖多 Python 版本的编译与测试。
